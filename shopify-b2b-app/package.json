{
  "name": "shopify-b2b-app",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "dev": "shopify app dev",
    "build": "shopify app build",
    "deploy": "shopify app deploy",
    "test": "vitest run --dir extensions",
    "typecheck": "tsc --noEmit -p extensions/b2b-checkout-lookup/tsconfig.json"
  },
  "devDependencies": {
    "@shopify/cli": "^3.70.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
